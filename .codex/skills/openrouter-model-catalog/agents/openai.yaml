interface:
  display_name: "OpenRouter Model Catalog"
  short_description: "Generate Codex OpenRouter model catalogs"
  default_prompt: "Use $openrouter-model-catalog to update my Codex OpenRouter model catalog."
policy:
  allow_implicit_invocation: false
